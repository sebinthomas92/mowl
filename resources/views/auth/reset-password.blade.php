<main class="auth-shell"><section class="auth-form-panel"><form method="POST" action="{{ route('password.update') }}" class="auth-form">@csrf<input name="token" type="hidden" value="{{ $token }}"><p class="kicker">ACCOUNT RECOVERY</p><h2>Choose a new password.</h2><label>Email address<input name="email" type="email" value="{{ old('email', $email) }}" required></label><label>New password<input name="password" type="password" required></label><label>Confirm password<input name="password_confirmation" type="password" required></label>@error('email')<small class="error">{{ $message }}</small>@enderror<button class="primary-button" type="submit">Reset password <span>→</span></button></form></section></main>
