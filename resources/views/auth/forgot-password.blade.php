<main class="auth-shell"><section class="auth-form-panel"><form method="POST" action="{{ route('password.email') }}" class="auth-form">@csrf<p class="kicker">ACCOUNT RECOVERY</p><h2>Reset your password.</h2><p>We will email a secure, one-hour reset link.</p>@if(session('status'))<p role="status">{{ session('status') }}</p>@endif<label>Email address<input name="email" type="email" value="{{ old('email') }}" required autofocus></label>@error('email')<small class="error">{{ $message }}</small>@enderror<button class="primary-button" type="submit">Email reset link <span>→</span></button><p class="auth-switch"><a href="{{ route('login') }}">Back to sign in</a></p></form></section></main>
